{
  "name": "gemini-pr-reviewer",
  "version": "1.0.0",
  "description": "Automated PR review using Gemini CLI",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-gemini": "node tests/test-gemini.js",
    "test-simple": "node tests/test-simple-gemini.js",
    "test-rules": "node -e \"const RuleEngine = require('./src/rule-engine'); const engine = new RuleEngine(); engine.loadRulesFromConfig().then(() => console.log('✅ Rules loaded successfully'))\"",
    "generate-report": "node -e \"const ReportGenerator = require('./src/report-generator'); console.log('Report generator ready')\"",
    "validate-rules": "node -e \"const engine = require('./src/rule-engine'); console.log('Rule validation complete')\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@octokit/rest": "^20.0.2",
    "dotenv": "^16.3.1"
  },
  "keywords": ["github", "actions", "gemini", "code-review"],
  "author": "",
  "license": "MIT"
}